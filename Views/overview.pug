extends base
include _tourCard.pug

block append head
    link(rel="stylesheet" href="/css/tourCard.css")

block content 
    main.main
        .card-container
            .u-center-text.u-margin-bottom-big.u-sticky
                h2.heading-secondary= "Here are available tours"

            each tour in tours
                +tourCard(tour)